# WebScrapper
A web scrapper that takes images and description from a website and saves it as a json file
At the moment the images are still in the relative url so it is using the base url of the website,
